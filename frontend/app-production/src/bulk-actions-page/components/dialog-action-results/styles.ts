import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  downloadButton: {
    margin: '5px',
  },
  dialogContent: {
    height: '400px',
  },
}));
