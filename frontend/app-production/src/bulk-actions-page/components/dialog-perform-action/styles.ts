import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  paper: { backgroundColor: '#f5f5f5' },
  scrollPaper: { flexDirection: 'column' },
  containerRoot: { overflowY: 'hidden' },
}));
