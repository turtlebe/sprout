import { Box, styled } from '@plentyag/brand-ui/src/material-ui/core';

export const StyledBox = styled(Box)({
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
  alignItems: 'center',
});
