import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  jsonContainer: {
    overflowY: 'auto',
    lineBreak: 'anywhere',
    whiteSpace: 'pre-wrap',
  },
}));
