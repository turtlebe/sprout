import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  diagramContainer: {
    width: '176px',
    height: '100%',
    position: 'relative',
  },
}));
