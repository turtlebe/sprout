import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  tooltipContent: {
    margin: 0,
    fontSize: '0.8rem',
    lineHeight: '1.15rem',
  },
}));
