import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  listItemRoot: {
    minWidth: '2rem',
  },
  italicTypography: {
    fontStyle: 'italic',
  },
}));
