import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  title: {
    alignSelf: 'center',
  },
  disableLink: {
    pointerEvents: 'none',
    cursor: 'default',
  },
}));
