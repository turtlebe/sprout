import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(theme => ({
  titleRoot: {},
  baseFormFooter: { padding: theme.spacing(2, 3) },
  paper: { backgroundColor: '#f5f5f5' },
  contentRoot: { padding: 0 },
}));
