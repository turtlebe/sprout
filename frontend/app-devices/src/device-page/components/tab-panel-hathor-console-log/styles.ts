import { makeStyles } from '@plentyag/brand-ui/src/material-ui/core';

export const useStyles = makeStyles(() => ({
  consoleLog: { whiteSpace: 'pre-line', maxHeight: 500, overflow: 'auto', fontFamily: 'Nunito' },
}));
